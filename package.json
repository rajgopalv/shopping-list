{
  "name": "shopping-list",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "mcp-server"
  ],
  "scripts": {
    "dev": "npm run dev -w server & npm run dev -w client & wait",
    "build": "npm run build -w client && npm run build -w server && npm run build -w mcp-server",
    "start": "npm run build && node server/dist/index.js",
    "test": "npm run test -w server",
    "mcp": "npm run start -w mcp-server"
  }
}
